# WebServ
Webserv is a custom HTTP/1.1 web server written in C++ as part of the 42 curriculum. It implements non-blocking I/O, supports multiple clients, configurable routing, CGI execution, and core HTTP methods, following Nginx-like behavior without external libraries.
