server {
    listen 8090;
    server_name a;
    root ./www;
    location / { allowed_methods GET; }
}

server {
    listen 9090;
    server_name b;
    root ./www;
    location / { allowed_methods GET; }
}
